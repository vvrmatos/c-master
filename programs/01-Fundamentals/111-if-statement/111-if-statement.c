#include <stdio.h>

int main(void) {
    int temperature = 30;
    
    if (temperature > 25) {
        printf("It's hot outside\n");
    }
    
    int x = 10;
    if (x)
        printf("x is non-zero\n");
    
    return 0;
}
